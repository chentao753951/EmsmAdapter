pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        // 引用部分外部github 仓库
        maven { url 'https://jitpack.io' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 引用部分外部github 仓库
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "EmsmAdapter"
include ':app'
include ':lib-emsm'
include ':lib-emsm-1'
